class: ComisionSolicitada
table: comision_solicitada
fields:
  idComision: int, relation(parent=comisiones)
  idUsuarioPendiente: int, relation(parent=usuarios_pendientes, onDelete=Cascade)
  ultimaModificacion: DateTime
  fechaCreacion: DateTime
  fechaEliminacion: DateTime?