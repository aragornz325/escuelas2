class: RelacionComisionUsuario
table: cursadas
fields:
  usuario: Usuario?, relation(name=usuario_cursadas)
  comision: ComisionDeCurso?, relation(name=comision_cursadas)
indexes:
  cursada_index_idx:
    fields: usuarioId, comisionId
    unique: true