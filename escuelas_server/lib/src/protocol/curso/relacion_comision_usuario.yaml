class: RelacionComisionUsuario
table: cursadas
fields:
  usuario: Usuario?, relation(name=usuario_comisiones)
  comision: ComisionDeCurso?, relation(name=comision_comisiones)
indexes:
  cursada_index_idx:
    fields: usuarioId, comisionId
    unique: true